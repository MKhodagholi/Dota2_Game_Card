let player;

function Player(health, mana, armor, str, agil, intel) {
    this.health = health;
    this.mana = mana;
    this.mana = mana;
    this.armor = armor;
    this.str = str;
    this.agil = agil;
    this.intel = intel;
}